import { CustomerApiService } from './customer-api-service';

describe('CustomerApiService', () => {
  it('should create an instance', () => {
    expect(new CustomerApiService()).toBeTruthy();
  });
});
