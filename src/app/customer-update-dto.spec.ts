import { CustomerUpdateDto } from './customer-update-dto';

describe('CustomerUpdateDto', () => {
  it('should create an instance', () => {
    expect(new CustomerUpdateDto()).toBeTruthy();
  });
});
