import { ShowroomSharedService } from './showroom-shared-service';

describe('ShowroomSharedService', () => {
  it('should create an instance', () => {
    expect(new ShowroomSharedService()).toBeTruthy();
  });
});
